{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "launch",
        "name": "Launch Jekyll Server",
        "program": "${workspaceFolder}/docker-compose.yml",
        "args": ["up"]
      }
    ]
}